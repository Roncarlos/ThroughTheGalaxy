-- ----------------------------------------------------------------------------
-- Configuration de l'application
-- ----------------------------------------------------------------------------

application =
{
  content =
  {
    width = 1080,        -- largeur de la zone de contenu (en mode portrait)
    height = 1920,       -- hauteur de la zone de contenu (en mode portrait)
    scale = 'adaptive', -- mise à l'échelle du contenu à l'écran
    fps = 60,           -- fréquence de raffraîchissement de l'écran
  }
}
